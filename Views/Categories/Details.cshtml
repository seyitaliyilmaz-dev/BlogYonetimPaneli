@* Tek bir kategorinin bilgilerini ve o kategoriye ait yazıları gösterir. *@
@model BlogYonetimPaneli.Models.Category

@{
    ViewData["Title"] = "Kategori Detay";
}

<div>
    <dl class="row">
        <dt class="col-sm-2">Ad</dt>
        <dd class="col-sm-10">@Model.Name</dd>

        <dt class="col-sm-2">Açıklama</dt>
        <dd class="col-sm-10">@Model.Description</dd>
    </dl>
</div>

@* Model.Posts, controller'da Include(c => c.Posts) ile birlikte yüklendiği için burada kullanılabilir. *@
<h4>Bu Kategorideki Yazılar</h4>
<ul>
@foreach (var post in Model.Posts)
{
    <li>@post.Title</li>
}
</ul>

<a asp-action="Edit" asp-route-id="@Model.Id">Düzenle</a> |
<a asp-action="Index">Listeye Dön</a>
